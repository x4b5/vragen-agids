# Engine — Pure Functions

- Alle bestanden in deze directory zijn **pure functies** (geen side effects)
- Lees altijd ALLE engine files voordat je er een wijzigt — ze zijn onderling afhankelijk
- Functies ontvangen input en retourneren output, geen state mutations
- Na wijziging: `npm run check` en `npm run test:run`
